# ChannelSpec

Specification, documentation, and contract generation for Phoenix Channels.

> [!WARNING]
> Early development. APIs are expected to change.

## Installation

Add `:channel_spec` to your list of dependencies in `mix.exs`:

```elixir
def deps do
  [
    {:channel_spec, "~> 0.1"}
  ]
end
```
